name: to_do
description: "A To-do App that use jsonplaceholder for mock API."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  get_it: ^7.6.2
  flutter_secure_storage: ^9.1.0
  http: ^1.1.1
  dartz: ^0.10.1
  flutter_svg: ^1.0.0
  animate_do: ^2.1.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true